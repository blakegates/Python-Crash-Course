users = ['admin', 'belle', 'bri', 'blake', 'jack', 'mosh']

for user in users:
    if user == 'admin':
        print(f"Hello admin, would you like to view the system reports?")
    else:
        print(f"Hello {user}.")